#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 * @g: integer input
 * Return: absolute value of g
 */
int _abs(int g)
	{
		if (g >= 0)
		{
			return (g);
		}
		return (-g);
	}

